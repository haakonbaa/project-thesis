\usepackage[utf8]{inputenc}            % For UTF-8 encoding
\usepackage{lmodern}                   % Modern fonts
\usepackage{geometry}                  % Page layout
\usepackage[pdftex,hidelinks]{hyperref}          % For hyperlinks
\usepackage{graphicx}                  % For including images
\usepackage{amsmath}                   % Math symbols and environments
\usepackage{amsfonts}                  % Math fonts
\usepackage{bm}                        % Bold math symbols
\usepackage{fancyhdr}                  % Custom headers and footers
\usepackage[inkscapelatex=false]{svg}  % For including SVG images
\usepackage{titlesec}                  % Custom section titles (Huge ones :)
\usepackage[style=ieee]{biblatex}      % IEEE citation style
\usepackage{setspace}
\usepackage[en-US]{datetime2}
\usepackage{listings}                  % For code listings
\usepackage{xcolor}                    % Required for specifying colors by name



\hypersetup{
    % Sets the displayed title
    pdftitle={Task-Priority Control of Underwater Vehicle-Manipulator Systems},    
    pdfauthor={Håkon Bårsaune},              % Sets the author
    pdfsubject={Task-Priority Control},      % Sets the subject
    pdfkeywords={LaTeX, Thesis}              % Sets keywords (optional)
}

% Format chapters to be displayed as "Chapter <number> - <title>" (titlesec)
\titleformat{\chapter}[display]
  {\normalfont\Huge\bfseries} % Format for "Introduction" (large and bold)
  {\LARGE\textbf{Chapter \thechapter}} % Format for "Chapter 1" (slightly smaller)
  {0pt} % Spacing between "Chapter 1" and "Introduction"
  {\vspace{1ex}} % Vertical space before the title

% Set spacing between lines (setspace)
\setstretch{1.2}

% Include the bibliography file (biblatex)
\addbibresource{refs.bib}
\addbibresource{tpc.bib}

% Set the output directory for converted files (svg)
\svgsetup{
    inkscapepath=../build/,
}
% Place numbers on the bottom of the page (fancyhdr)
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt} % Remove the horizontal line in the header

% Custom commands
\newcommand{\SO}{\operatorname{SO}(3)}
\newcommand{\so}{\mathfrak{so}(3)}
\newcommand{\SE}{\operatorname{SE}(3)}
\newcommand{\se}{\mathfrak{se}(3)}
\newcommand{\Ad}{\operatorname{Ad}}
\newcommand{\ad}{\operatorname{ad}}
\newcommand{\pymuvs}{\texttt{PyMUVs}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\I}{\mathbb{I}}
% matrix version of cross product
\newcommand{\cross}[1]{[ #1 ]_{\times}}

% Code snippets settings ------------------------------------------------------
% Change the name of the list of listings
\renewcommand{\lstlistlistingname}{List of Code Examples}

% Use the inconsolata font for monospaced text (listings)
\usepackage{inconsolata}
\renewcommand{\ttdefault}{zi4}


% Define a custom style
\lstdefinestyle{custompython}{
    backgroundcolor=\color{gray!0},   % Light gray background
    commentstyle=\color{green!60!black}, % Green comments
    keywordstyle=\color{blue},        % Blue keywords
    stringstyle=\color{orange},       % Orange strings
    basicstyle=\ttfamily\footnotesize,% Monospaced font, smaller size
    inputencoding=utf8,
    breakatwhitespace=false,          
    breaklines=true,                 
    captionpos=b,                     
    keepspaces=true,                 
    numbers=left,                     % Line numbers on the left
    showspaces=false,                 
    showstringspaces=false,           
    showtabs=false,                   
    tabsize=4,
    rulecolor=\color{black!20},        % Light gray frame
    language=Python
}

\lstdefinestyle{customcpp}{
    language=C++,                  % Set language to C++
    backgroundcolor=\color{gray!0},% Light gray background
    commentstyle=\color{green!50!black}, % Greenish comments
    keywordstyle=\color{blue},     % Blue keywords
    stringstyle=\color{orange},    % Orange strings
    basicstyle=\ttfamily\footnotesize, % Monospaced small font
    rulecolor=\color{black!30},    % Light gray frame
    tabsize=4,                     % Tab size
    showstringspaces=false,        % Don't show spaces in strings
    showspaces=false,              % Don't show spaces
    showtabs=false,                % Don't show tabs
    breaklines=true,               % Enable line breaking
    breakatwhitespace=false,       % Break lines at any character
    numbers=left,                  % Line numbers on the left
    numbersep=10pt,                % Spacing between numbers and code
    captionpos=b,                  % Place captions below the code
    escapeinside={(*@}{@*)},       % Escape LaTeX code inside listings
    extendedchars=true,            % Allow extended characters
    keepspaces=true                % Keep spaces in code
}

\lstdefinestyle{customtxt}{
    backgroundcolor=\color{gray!0},% Light gray background
    basicstyle=\ttfamily\footnotesize, % Monospaced small font
    rulecolor=\color{black!30},    % Light gray frame
    tabsize=4,                     % Tab size
    showstringspaces=false,        % Don't show spaces in strings
    showspaces=false,              % Don't show spaces
    showtabs=false,                % Don't show tabs
    breaklines=true,               % Enable line breaking
    breakatwhitespace=false,       % Break lines at any character
    numbers=left,                  % Line numbers on the left
    numbersep=10pt,                % Spacing between numbers and code
    captionpos=b,                  % Place captions below the code
    extendedchars=true,            % Allow extended characters
    keepspaces=true                % Keep spaces in code
}

\lstset{
    extendedchars=true,
    inputencoding=utf8,
    framerule=1pt,
    frame=single, %leftline,
    numberstyle=\color{gray},
    tabsize=4,
    lineskip=-1pt,
    literate=
        {ϕ}{{$\phi$}}1
        {θ}{{$\theta$}}1
        {ψ}{{$\psi$}}1
        {τ}{{$\tau$}}1
}
